# AnimatedCounter → ⚠️ DEPRECATED
Dùng `AnimatedCounter` từ `remotion-bits` thay thế.

```tsx
import { AnimatedCounter } from "remotion-bits";

<AnimatedCounter from={0} to={10000} duration={60}
  style={{ fontSize: 72, fontWeight: 800, color: COLORS.accent }} />
```

Xem thêm: [../references/remotion-libraries.md](../references/remotion-libraries.md)
